# Hermes Agent 도입 설계

> **AIOps Dashboard에 "프로젝트 위키 자동 생성기"를 내장한다.**
> 바이브코딩으로 누적된 로컬 도메인 자산을 Hermes Agent가 조합·요약해
> 기획자/개발자/사용자 3-perspective 위키로 만들고 개인 데이터화한다.

작성일: 2026-05-18 (최초) / 2026-05-19 (사용자 의도 명확화 후 핵심 재정의)
참고: https://wikidocs.net/339242 (Claude Code → Hermes 인계)

---

## 1. 사용자가 정의한 문제

> "신규 프로젝트 초기에는 어떤 소스도 존재하지 않는다. 그러나 몇 개월 바이브코딩하다 보면 점점 체계가 잡히고 문서도 생기는데, 그걸 로컬에만 두고 Claude Code로만 보기에는 매번 토큰 소모가 비효율적이다. **Hermes Agent를 활용해 업데이트 버튼 같은 걸 만들어, 로컬에 있는 소스들을 조합해서 기획자·개발자·사용자 측면을 고려한 위키를 하나 만드는 게 목표.** 또한 그걸 개인 데이터화해서 관리하는 것."

문제 4개를 한 번에 해결:

| 문제 | Hermes 위키의 답 |
|---|---|
| 매번 Claude Code에 동일 컨텍스트 다시 설명 | 위키 1장만 첨부 |
| 로컬 자산이 흩어져 검색·재활용 불가 | 3-perspective 위키로 정리·인덱싱 |
| 본인도 며칠 뒤 길 잃음 | 위키가 최신 상태로 유지 |
| 외부에 공유할 자료 부재 | 그대로 Notion/README로 export |

---

## 2. 실제 데이터 사례 — ERP 프로젝트 (`/mnt/c/project/inconus-api-erp-v2`)

사용자가 6개월간 바이브코딩으로 쌓아둔 도메인 자산:

```
inconus-api-erp-v2/
├── CLAUDE.md                  (10.7KB) ← 프로젝트 메타
├── HANDOFF.md, HELP.md        ← 사용자 가이드
├── handoff/
│   ├── INDEX.md, main.md, hotfix.md, infra.md
│   └── archive/HANDOFF_legacy.md
├── src/
│   ├── docs/ERROR_MESSAGES.md  ← 도메인 명세
│   └── sql/README.md
└── .claude/
    ├── rules/   (7개)   ★ 도메인 노하우 핵심
    │   ├── 예외처리_가이드.md     (Spring Boot + Sentry 4계층 분류)
    │   ├── 워크플로_가이드.md
    │   ├── 코드스타일_가이드.md
    │   ├── 테스트_가이드.md
    │   ├── 문서_템플릿_가이드.md
    │   ├── 스토리보드_분석.md
    │   └── 에이전트_활용_가이드.md
    ├── agents/  (5개)
    │   ├── convention-detective.md     (코드 컨벤션 점검)
    │   ├── quality-api-code-review.md
    │   ├── workflow-session-start.md
    │   ├── demo-code-reviewer/
    │   └── ops-hotfix-pipeline/
    └── skills/  (6개)
        ├── docs-generate.md, docs-guide.md
        ├── git-commit-push.md, intg-notion.md
        ├── find-skills/, karpathy-guidelines/
```

**이게 신규 도메인 작업·수정 시 효율의 원천이다.** Hermes 위키는 이 자산을 자동으로 읽고 통합하여 1장의 활용 가능한 산출물로 만든다.

---

## 3. Hermes Agent — 우리 컨텍스트에서의 정의

```
                ┌─ [원본 Hermes 차용] ─┐
                │  - 프로젝트별 분리   │
                │  - 지속 메모리       │
                │  - SOUL.md 컨셉      │
                └──────────────────────┘
                          │
                          ▼
        ┌─ AIOps 특화: 프로젝트 위키 생성기 (MVP) ─┐
        │  입력: 로컬 .md 자산 + 우리 활동 로그   │
        │  처리: 카탈로그 → 분류 → LLM 요약·조합 │
        │  출력: 3-perspective 위키 (마크다운)    │
        │  저장: hermes_wikis 테이블 + 버전관리  │
        └──────────────────────────────────────────┘
                          │
                          ▼
              [장기 확장: 능동 코치, 패턴 인식, 알림 등]
```

---

## 4. 입력 → 처리 → 출력 흐름

```
[입력]                                  [처리]                          [출력]

로컬 자산                       Hermes Engine                    3-perspective 위키
├── *.md (재귀 스캔)            ┌──────────────────┐            ┌──────────────────┐
│   (CLAUDE.md, handoff/,      │ 1. 자산 카탈로그 │            │ A. 기획자 챕터   │
│    .claude/rules/,           │    수집·분류     │  ────────> │ B. 개발자 챕터   │
│    .claude/agents/,          │ 2. perspective    │            │ C. 사용자 챕터   │
│    .claude/skills/,          │    매핑          │            └──────────────────┘
│    src/docs/)                │ 3. LLM 요약·조합 │                    │
│                              │    (Claude API)   │                    ▼
└── 우리 활동 로그 (선택)      │ 4. 메모리 누적   │            hermes_wikis 테이블
    (prompts, sessions,        └──────────────────┘            (tenant + project별
     agents 카테고리)                                           버전 관리)
```

**핵심 트리거**: 사용자가 "에이전트" 탭에서 **`[Hermes 업데이트]`** 버튼 클릭 → 백그라운드 작업으로 위 흐름 실행 → 완료 시 알림.

---

## 5. 3-perspective 위키 구조 (입력 매핑 포함)

각 챕터별로 어떤 자료가 입력되는지 명시. ERP 사례 기준.

### A. 기획자 챕터

| 섹션 | ERP 자산 기준 입력 소스 |
|---|---|
| 프로젝트 한 줄 정의 | CLAUDE.md 첫 단락 |
| 비전·도메인 맥락 | CLAUDE.md "프로젝트 개요" |
| 핵심 사용자 시나리오 | handoff/main.md + 스토리보드_분석.md |
| 의사결정 기록 | handoff/archive/* + Git 커밋 메시지 패턴 |
| 진행 상황 (로드맵) | handoff/INDEX.md + handoff/main.md |
| 성공 지표 / 메트릭 | (없으면 빈 칸 — Hermes가 추후 제안) |

### B. 개발자 챕터

| 섹션 | ERP 자산 기준 입력 소스 |
|---|---|
| 기술 스택 | CLAUDE.md "기술 스택" + build.gradle.kts |
| 아키텍처 | CLAUDE.md + 워크플로_가이드.md |
| 도메인 규칙 | .claude/rules/* 전체 (예외처리·코드스타일·테스트·워크플로) |
| 에이전트 활용 | .claude/agents/* 정의 + 에이전트_활용_가이드.md |
| 스킬(슬래시 명령) | .claude/skills/* 전체 |
| 데이터/SQL | src/sql/README.md |
| 에러 메시지 카탈로그 | src/docs/ERROR_MESSAGES.md |
| 인프라/배포 | handoff/infra.md + Dockerfile |
| 긴급 대응 절차 | handoff/hotfix.md |

### C. 사용자 챕터

| 섹션 | ERP 자산 기준 입력 소스 |
|---|---|
| 빠른 시작 | HELP.md |
| 핵심 기능 사용법 | docs-generate.md 등 스킬에서 사용자 관점 추출 |
| 문서 작성 가이드 | 문서_템플릿_가이드.md |
| FAQ | (자산에 없으면 활동 로그의 반복 질문 패턴에서 추론) |
| 트러블슈팅 | handoff/hotfix.md + ERROR_MESSAGES.md |

---

## 6. 자산이 쌓일수록 효율이 커지는 메커니즘 (강조)

```
신규 프로젝트 (M=0)        M=2개월         M=6개월          M=12개월
   소스 0                  소스 30개       소스 80개        소스 150개
        │                       │                │                │
        ▼                       ▼                ▼                ▼
Claude 작업: 매번      위키 v1                위키 v3            위키 v8
컨텍스트 새로 만듦     (얇음)                 (풍부함)           (정련됨)
        │                       │                │                │
        ▼                       ▼                ▼                ▼
토큰 소모: 100%        50%                    30%                15%
효율: 낮음             중간                   높음               매우 높음

  ※ 신규 도메인 작업 시작 시 — 위키만 첨부하면 즉시 컨텍스트 확보
  ※ 수정 작업 — 관련 챕터만 추출해서 첨부
  ※ 이게 사용자가 말한 "쌓일수록 효율적" 의 메커니즘
```

---

## 7. DB 스키마

```sql
CREATE TABLE hermes_wikis (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tenant_id    TEXT NOT NULL,
    project_name TEXT NOT NULL,
    perspective  TEXT NOT NULL,   -- 'planner'|'developer'|'user'
    content      TEXT NOT NULL,   -- 마크다운 챕터 본문
    version      INTEGER DEFAULT 1,
    updated_at   TEXT DEFAULT (datetime('now')),
    UNIQUE(tenant_id, project_name, perspective, version)
);

CREATE TABLE hermes_wiki_runs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tenant_id    TEXT NOT NULL,
    project_name TEXT NOT NULL,
    status       TEXT,            -- 'running'|'success'|'failed'
    started_at   TEXT DEFAULT (datetime('now')),
    finished_at  TEXT,
    input_count  INTEGER,         -- 처리한 입력 파일 수
    output_chars INTEGER,         -- 생성된 위키 글자 수
    error        TEXT,
    diff_summary TEXT             -- "이번 갱신에서 무엇이 추가/변경되었나"
);

-- 장기 확장용 (지금은 미사용, 스키마만 예약)
CREATE TABLE hermes_souls (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    tenant_id TEXT NOT NULL,
    project_name TEXT NOT NULL,
    soul TEXT,
    updated_at TEXT DEFAULT (datetime('now')),
    UNIQUE(tenant_id, project_name)
);
```

---

## 8. UI 설계

### "에이전트" 탭 (신규)

```
┌─ 에이전트 (Hermes) ──────────────────────────────────────┐
│                                                          │
│ 프로젝트: [inconus-api-erp-v2 ▾]                         │
│                                                          │
│ ┌─ 위키 ──────────────────────────────────────────────┐ │
│ │ 최근 갱신: 2026-05-19 11:32  v3  ⟳ 마지막 35초     │ │
│ │                                                     │ │
│ │   [기획자] [개발자] [사용자]   ← 챕터 탭            │ │
│ │ ─────────────────────────────────────               │ │
│ │  (선택한 챕터 마크다운 렌더링)                      │ │
│ │  ...                                                │ │
│ │                                                     │ │
│ │ [Hermes 업데이트]  [내보내기 ▾]  [버전 히스토리]    │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                          │
│ ┌─ 마지막 갱신 정보 ─────────────────────────────────┐ │
│ │ 입력: 23개 파일 (rules 7, agents 5, skills 6, etc) │ │
│ │ 변경: + 코드스타일_가이드.md (신규 자산 감지)      │ │
│ │       ~ 워크플로_가이드.md (15줄 변경)             │ │
│ │ 소요: 38초 (LLM 호출 12회)                         │ │
│ └─────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘
```

### 기존 페이지의 변화

- **TopBar 우측 ConnectionStatus 옆**에 "Wiki" 작은 배지 (최근 갱신 N일 전)
- **시스템 상태 페이지** 상단에 위키 갱신 추천 카드 (자산 변동 감지 시)

---

## 9. MVP Phase 분할

### Phase 1 — 인프라 + 카탈로그 수집 (1일)

- DB 스키마 3개 신설 (hermes_wikis, hermes_wiki_runs, hermes_souls)
- `services/hermes/` 모듈 골격 (catalog.py, wiki_builder.py)
- 활성 프로젝트의 `repoPath`에서 .md 파일을 재귀 스캔하는 `catalog_assets()` 함수
- "에이전트" 탭 신규 (빈 UI, 카탈로그 결과 표시)
- 백엔드 라우터 `POST /api/hermes/catalog` (스캔만, 위키 생성 없음)

### Phase 2 — Hermes 업데이트 v1 — 위키 생성 (1~1.5일)

- `wiki_builder.py`에 perspective별 입력 매핑
- LLM 호출(Claude API) 래퍼 (사용자 본인 키 활용)
- `POST /api/hermes/wiki/update` (활성 프로젝트에 대해 위키 3챕터 생성)
- "Hermes 업데이트" 버튼 + 진행 상태 표시 (WebSocket)
- 결과 저장 + 마크다운 렌더링 UI

### Phase 3 — 버전 관리 + 변경 감지 (0.5일)

- 새 갱신마다 version +1
- `hermes_wiki_runs.diff_summary`에 "이번에 무엇이 추가/변경됐나" 자동 작성
- "버전 히스토리" UI

### Phase 4 — 활동 로그 결합 (0.5일)

- 우리가 모은 prompts/hooks/sessions 카테고리도 입력에 추가
- FAQ 섹션을 활동 패턴에서 추론
- 챕터별 입력 매핑에 활동 로그 추가

### Phase 5 — Export / 검색 (0.5일)

- Notion export (기존 MCP 활용)
- README.md 모드 export
- 위키 본문 검색 (SQLite FTS5)

### Phase 6+ — 장기 확장 (별도)

- 능동 코치 (프롬프트 개선 제안 push)
- 자동화 제안 (반복 패턴 → 슬래시 명령 제안)
- 24/7 이상 감지 + 알림
- 우하단 ChatBot을 Hermes로 격상 (위키를 컨텍스트로 Q&A)
- 메시징 게이트웨이 (Telegram/Slack)

→ Phase 1~5로 약 3~4일이면 사용자가 직접 가치를 느끼는 MVP 완성.

---

## 10. 기존 시스템과의 연결

```
┌─ AIOps Dashboard (기존) ───────────────────────────────┐
│                                                        │
│  Hook → ingest → DB(logs)                              │
│                                                        │
│  projects 테이블 ── repoPath ──> [로컬 .md 파일]      │
│                                          │             │
│                                          │ 스캔        │
│                                          ▼             │
│  ┌─ Hermes Engine (신규) ───────────────────────────┐ │
│  │                                                  │ │
│  │  catalog.py        → 자산 목록 (perspective별) │ │
│  │     │                                            │ │
│  │     ▼                                            │ │
│  │  wiki_builder.py   → LLM 호출 → 챕터 작성       │ │
│  │     │                                            │ │
│  │     ▼                                            │ │
│  │  hermes_wikis      → DB 저장 + version          │ │
│  │     │                                            │ │
│  │     ▼                                            │ │
│  │  routers/hermes.py → GET/POST API               │ │
│  └─────────────────────────────────────────────────┘ │
│                                                        │
│  pages/Agent.tsx ──── 위키 렌더링 + 업데이트 버튼      │
└────────────────────────────────────────────────────────┘
```

- 기존 인프라(projects 테이블, repoPath, tenant 격리, ingest)를 그대로 활용
- 신규는 services/hermes/ 모듈과 hermes_wikis 테이블, /api/hermes/* 라우터, /Agent 페이지 4개

---

## 11. 의사결정 사항

| 항목 | 결정 |
|---|---|
| **LLM API 비용** | 사용자 본인 ANTHROPIC_API_KEY를 ~/.claude/.env에서 가져옴. 셀프 호스팅과 일치 |
| **위키 저장 위치** | DB(hermes_wikis 테이블). 파일이 아닌 이유: 검색/버전관리/멀티테넌트 분리 용이 |
| **출력 형태** | 마크다운. Export 시 Notion/README로 변환 |
| **자율 실행 권한** | Hermes는 **읽기만**. 사용자가 명시적으로 `[업데이트]` 버튼 클릭 시에만 동작. 자동 cron은 Phase 6+ |
| **다국어** | 한국어 우선 (사용자 자산이 한글). Hermes 출력도 한국어 |
| **로컬 파일 접근** | 단일 머신 셀프 호스팅 가정. repoPath의 .md를 서버가 직접 read |
| **시크릿 마스킹** | 위키 생성 결과에도 secret_masker 적용 (혹시 LLM 출력에 토큰이 섞이면) |

---

## 12. 다음 작업 (이 설계가 승인되면)

1. `프로젝트-기획서.md` 다시 갱신 — 비전을 "위키 생성기"로 더 뾰족하게
2. Phase 1 구현 시작:
   - DB 마이그레이션 (hermes_wikis, hermes_wiki_runs, hermes_souls)
   - `services/hermes/catalog.py` — 재귀 .md 스캔 + 분류 함수
   - `routers/hermes.py` — `POST /api/hermes/catalog`
   - `pages/Agent.tsx` — 빈 위저드 UI, 카탈로그 결과만 표시
3. ERP 프로젝트로 검증 (스캔 결과 25+ 자산 식별돼야 정상)

---

## 13. 검토 체크리스트 (사용자 검토)

- [ ] 사용자가 묘사한 시나리오와 일치하는가? (위키 = 신규 도메인 작업 시 컨텍스트 줄이는 도구)
- [ ] ERP 자산 카탈로그가 실제 본인 데이터와 맞는가?
- [ ] 3-perspective 매핑이 적절한가? 다른 자산을 어느 챕터에 넣을지 추가/이동할 것이 있나?
- [ ] MVP 범위가 합리적인가? (Phase 1~5, 약 3~4일)
- [ ] 빠진 기능이 있나? (예: 위키 부분 편집, 수동 보강, 외부 자산 import 등)
