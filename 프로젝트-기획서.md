# AIOps Dashboard — 프로젝트 기획서

> **작성일**: 2026-05-12 (최초) / 2026-05-19 (Hermes 위키 생성기로 비전 명확화)
> **상태**: SaaS 모드 기반 동작 검증 완료, MVP 고도화 단계 진입
> **목적**: 외부 Claude 유저에게 본 서비스를 설명하기 위한 자료
> **연계 문서**: `헤르메스-도입-설계.md`

---

## 1. 비전

> **Claude 유저의 사용 패턴을 추적하고, 프로젝트별로 누적된 로컬 자산을 Hermes Agent가 위키로 정리해 다음 작업의 토큰 효율을 끌어올리는 메타-대시보드.**

Claude는 Anthropic이 끊임없이 개선한다. 그러나 결국 도구를 쓰는 건 사람이고, **사람의 능력 + 사람이 만든 도메인 자산이 누적되지 않으면 모델이 아무리 좋아져도 결과는 그대로**다. 본 프로젝트는 그 빈자리를 세 단계로 채운다:

1. **수동 단계 (Track)**: 사용자가 자기 활동을 대시보드에서 확인
2. **누적 단계 (Wikify via Hermes)**: 바이브코딩으로 쌓이는 로컬 자산(.claude/rules·agents·skills, handoff, CLAUDE.md 등)을 Hermes가 기획자/개발자/사용자 3-perspective 위키로 자동 통합
3. **능동 단계 (Coach via Hermes, 장기)**: 위키와 활동 데이터를 기반으로 능동 제안·자동화

→ 사용자 표현: *"몇 개월 바이브코딩하다 보면 체계와 문서가 쌓이는데, 그걸 로컬에만 두고 매번 Claude로 읽으면 토큰 비효율. Hermes가 자동 조합해 위키를 만들면 다음 신규 도메인이나 수정 작업이 점점 효율적이 된다."*

---

## 2. 미션

Claude 유저가:

- 자신이 쓴 **모든 프롬프트**를 데이터로 축적하고
- 어떤 **에이전트·Hook·도구**를 언제 어떻게 썼는지 자동 추적해
- **프롬프트 작성 능력**과 **AI 활용 능력**을 데이터 기반으로 개선한다.

---

## 3. 타겟 사용자

- Claude Code를 매일 사용하는 **개발자/지식 노동자**
- 본인의 AI 사용 패턴을 **객관적으로 보고 개선**하고 싶은 사람
- 팀 단위로 **Claude 활용 수준을 끌어올리고 싶은 매니저**

---

## 4. 제품 핵심 가치 (4단)

| 가치 | 설명 | 담당 |
|---|---|---|
| **추적 (Track)** | Claude 사용의 모든 흔적(프롬프트·Hook·에이전트·슬래시·MCP) 자동 수집 | 기존 인프라 |
| **분석 (Analyze)** | 프롬프트 품질·시간대 패턴·도구 활용도 등 학습 지향 지표 | Phase B + #4 |
| **코치 (Coach)** | 프로젝트별 Hermes Agent가 능동적으로 개선 제안 push | **Hermes (신설)** |
| **자동화 (Automate)** | 반복 패턴 감지 → 슬래시 명령·스크립트로 자동화 추천/실행 | **Hermes (신설)** |

---

## 5. 차별점

| 기존 도구 | 한계 | 본 서비스의 차별점 |
|---|---|---|
| LangSmith / Helicone | LLM 호출 모니터링 (수동적) | **능동적 코치 + 자동화** (Hermes) |
| IDE 통계 | 활동 시간/커밋 위주 | **AI 활용 품질 + 프로젝트별 메모리** |
| 메모리/노트앱 | 정보 저장만 | **에이전트가 분석해서 제안 push** |
| 원본 Hermes Agent | 범용 프레임워크, 데이터 소스 직접 구성 필요 | **AIOps 데이터를 연료로 즉시 동작** |

---

## 6. 기능 백로그 (우선순위)

### 6.1 MVP 핵심 (지금 집중)

| 순위 | 영역 | 핵심 작업 | 상태 |
|---|---|---|---|
| 1 | **온보딩** | 5단계 위저드 | ✅ 구현 |
| 2 | **페이지 정리** | 무의미한 페이지 제거/재해석 | 🟡 부분 |
| 3 | **추적 확장** | SessionStart/Stop/SubagentStop + 슬래시 + MCP | 🟡 수집 ✅, 표시 미 |
| 4 | **프롬프트 분석 v1** | 길이·주제·시간대·추세 시각화 | □ |
| 5 | **개인 데이터 관리** | export/delete/통계/보존기간 | □ |
| **6** | **Hermes Agent 통합 (NEW)** | 프로젝트별 능동 코치 + 자동화. `헤르메스-도입-설계.md` 참고 | □ |

권장 전개: #3 Phase B 완료 → #6 Phase 1~2(인프라 + 프롬프트 코치) → #4와 자연스럽게 합류 (Hermes Phase 2가 #4의 진화형)

### 6.2 외부 공개 직전 한꺼번에

- **보안 강화**: bcrypt 비밀번호, rate limiting, CSRF, 환경변수 검증
- **운영 견고성**: 구조화 로깅, DB 마이그레이션, 백업/복구, 헬스체크 확장
- **품질**: pytest, vitest, E2E 테스트, CI/CD 파이프라인
- **확장성**: 모바일 반응형, README, OpenAPI 문서

---

## 7. 첫 단계 상세 — 온보딩 위저드

### 7.1 목표

가입 후 **5분 안에** 본인 작업이 대시보드에 흐른다.

### 7.2 현재 마찰점 (7개)

1. 가입 직후 빈 대시보드 → 다음 행동 모름
2. 탭 9개 중 "연결 설정" 위치 모름
3. API 키 일회성 노출 — 놓치면 재발급해야 함
4. `~/.claude/.env` 수동 편집 (기존 내용과 충돌 가능)
5. 본인 레포 경로 찾기 부담
6. 터미널 미숙 유저에게 `curl` 명령은 장벽
7. Hook이 잘 도는지 확인할 방법 없음

### 7.3 위저드 흐름 (5단계)

```
Step 1  환경 선택            Mac / Linux / WSL / Windows
Step 2  API 키 발급         자동 생성 + 큰 복사 버튼
Step 3  .env 자동 패치       환경별 한 줄 명령 (클립보드 복사)
Step 4  레포 등록 + Hook 설치  경로 입력 → 한 줄 설치 명령
Step 5  연결 자동 감지        WebSocket으로 첫 Hook 데이터 대기 → 도착 시 ✓
```

### 7.4 부가 개선

- 대시보드 상단 **연결 상태 위젯** (최근 24h 데이터 유무로 ON/OFF 표시)
- 빈 데이터 페이지에서 **"온보딩 다시 보기"** 링크 노출

### 7.5 구현 범위

- **신규 컴포넌트**: `OnboardingWizard.tsx`, `ConnectionStatus.tsx`
- **신규 API**: `POST /api/projects` (간소화 레포 등록), `GET /api/onboarding/status`
- **수정**: `App.tsx`(가입 직후 위저드 표시), `Connections.tsx`(콘텐츠 공유)
- **예상 공수**: 0.5 ~ 1일

---

## 8. 현재 구현 상태 (2026-05-12 기준)

- SaaS 모드 (멀티 테넌트, JWT 인증, API 키 발급, ingest API) 동작 검증 완료
- Hook 스크립트 (로컬 백업 + 원격 전송 dual mode) 동작 검증 완료
- WebSocket 실시간 푸시 (tenant별 격리) 동작 검증 완료
- 8개 페이지 기본 동작
- 설치 스크립트 정적 서빙 (`/api/scripts/install-hook.sh`)
- 위 백로그 1~5번은 미진행

---

## 9. 기술 스택

| 영역 | 기술 |
|---|---|
| 백엔드 | Python 3.11+ / FastAPI / Uvicorn / WebSocket / SQLite(aiosqlite) / watchfiles |
| 프론트엔드 | Vite + React 19 + TypeScript |
| 인증 | JWT (HS256, 24h) + API 키 (SHA256 해시 저장, 평문 1회 노출) |
| 데이터 (SaaS) | SQLite — `tenants`, `users`, `logs`, `projects` 4테이블 |
| 데이터 (Local) | JSONL — `.aiops/{hooks,prompts,workflow}/{날짜}.jsonl` |

상세는 `CLAUDE.md` 참고.

---

## 10. 운영 모드

| 모드 | 용도 | 데이터 흐름 |
|---|---|---|
| `local` | 개인 PC 단독 사용 | JSONL 파일 직접 읽기 + watchfiles 감시 |
| `saas` | 멀티 사용자 / 클라우드 배포 | Hook이 HTTP POST → DB 저장 → WebSocket 푸시 |

`AIOPS_MODE` 환경변수로 전환. 본 기획서는 **`saas` 모드 기준 MVP**를 다룬다.

---

## 11. 성공 지표 (MVP)

- **온보딩 완료율**: 가입자 중 첫 Hook 데이터가 5분 내 도달한 비율 ≥ 80%
- **재방문율**: 가입 후 7일 내 재로그인한 사용자 비율 ≥ 50%
- **추적 커버리지**: 단일 세션에서 수집되는 이벤트 종류 ≥ 6종 (현재 2종 — Hook, Prompt)
- **프롬프트 분석 활용**: 프롬프트 탭 진입 사용자 중 분석 위젯 사용 비율 ≥ 30%
