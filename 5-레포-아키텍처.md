# AI 개발 자동화 시스템 — 5-레포 아키텍처

> 최종 수정: 2026-03-07
> 상태: 아키텍처 확정

---

## 핵심 원칙

```
도메인이 바뀌면 프로젝트 레포만 교체한다.
나머지 4개 레포는 재사용된다.
```

---

## 전체 구조

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│  ① 거버넌스 레포                                                            │
│     글로벌 규칙 / Hook / 설정 / 권한                                         │
│     ─────────────────────────────────────────────────                       │
│     모든 레포가 이 레포의 규칙을 따름                                         │
│                                                                             │
├──────────────┬──────────────┬──────────────┬────────────────────────────────┤
│              │              │              │                                │
│  ④ 프로젝트  │  ③ 지식      │  ② 테스트    │  ⑤ RAG (챗봇)                  │
│  레포        │  레포         │  레포        │  레포                          │
│              │              │              │                                │
│  코드 작성   │  문서 저장    │  테스트 실행  │  챗봇 서비스                    │
│  (API/Front) │  (기능명세    │  (시나리오    │  (지식 레포 기반                │
│              │   아키텍처    │   결과 관리   │   Q&A 챗봇)                    │
│              │   인사이트    │   환경별 설정) │                               │
│              │   트러블슈팅  │              │                                │
│              │   스토리보드  │              │                                │
│              │   사용자매뉴얼│              │                                │
│              │   API 플로우) │              │                                │
│              │              │              │                                │
└──────────────┴──────────────┴──────────────┴────────────────────────────────┘

```

---

## 레포별 역할

### ① 거버넌스 레포

**역할:** 전체 시스템의 규칙, Hook, 설정, 권한을 중앙에서 관리

| 관리 대상 | 내용 |
|----------|------|
| 글로벌 CLAUDE.md 규칙 | 모든 레포에 적용되는 공통 규칙 |
| Hook 스크립트 | PreToolUse, PreCompact, Stop 등 |
| settings.json 템플릿 | 환경별 권한, 플러그인, 모델 설정 |
| 권한 매트릭스 | 레포 간 읽기/쓰기 권한 정의 |
| 프롬프트 로깅 Hook | 사용자 입력 프롬프트를 자동 저장 |
| PR 리뷰 워크플로우 | GitHub Actions YAML (Codex + Claude) |

**이 레포는 다른 레포에서 가져다 쓰는 "인프라" 레포다.**

---

### ② 테스트 레포

**역할:** AI가 작성한 코드를 체계적으로 테스트하고 결과를 관리

| 항목 | 내용 |
|------|------|
| 참조 | 프로젝트 레포 (읽기 전용), 지식 레포 (읽기 전용) |
| 환경 설정 | config/{local,dev,prod}.md — `.env` + `envsubst`로 생성 |
| 테스트 도구 | local: Swagger (curl), dev: curl, prod: Claude Chrome (Sonnet) |
| 결과 저장 | results/{환경}/{날짜}/{swagger-tag}.md |
| 규칙 | .claude/rules/ — 테스트 절차, 시나리오, 결과 기록, 워크플로우 |
| 사람 개입 | 테스트 템플릿이 제대로 동작하지 않을 때만 프롬프트 입력 |

**테스트 레포는 대부분 자동으로 돌아간다. 사람이 개입하는 순간은 최소화한다.**

---

### ③ 지식 레포

**역할:** 코드에 대한 지식을 문서화하여 AI의 "장기 기억"으로 활용

| 문서 유형 | 설명 | 생성 시점 |
|----------|------|----------|
| 기능명세서 | API 명세, 데이터 구조, 비즈니스 로직 | 기능 개발 완료 시 |
| 아키텍처 설명서 | 전체 흐름 다이어그램, 설계 결정 | 기능 개발 완료 시 |
| 사용자매뉴얼 | 화면별 사용법 | 기능 개발 완료 시 |
| API 플로우 | 엔드포인트 간 호출 흐름 | 기능 개발 완료 시 |
| 트러블슈팅 | 이슈 원인/해결 기록 | 버그 해결 시 |
| 인사이트 | 도메인 지식, 업무 노하우 | 수시 |
| 기획 스토리보드 | 기획 단계 문서 | 기획 시 |

**핵심:** 새로운 세션에서 AI가 기존 코드베이스를 이해할 때 이 레포를 참조한다.
프로젝트 레포의 코드만으로는 맥락을 알 수 없는 "왜 이렇게 만들었는가"를 여기서 해결한다.

---

### ④ 프로젝트 레포

**역할:** 실제 서비스 코드가 작성되는 곳

| 항목 | 내용 |
|------|------|
| 코드 | API 서버, 프론트엔드, 배포 설정 등 |
| 프롬프트 | 대부분의 사용자 프롬프트가 여기서 발생 |
| 문서 생성 | 코드 작성 → 지식 레포에 문서 자동 생성 (Hook) |
| 테스트 연동 | 코드 변경 → 테스트 레포에서 자동 테스트 |
| 도메인 교체 | 새 서비스 = 새 프로젝트 레포. 나머지 4개는 그대로 |

**도메인이 바뀔 때 유일하게 교체되는 레포.**

---

### ⑤ RAG 레포 (챗봇)

**역할:** 지식 레포의 문서를 기반으로 Q&A 챗봇 서비스를 제공

| 항목 | 내용 |
|------|------|
| 코어 데이터 | 지식 레포의 문서 (기능명세서, 아키텍처, 매뉴얼 등) |
| 구조 | 기본 RAG 파이프라인 (인덱싱 → 검색 → 생성) |
| 역할 | 프로젝트 레포의 코드를 뒷받침하는 지식 서비스 |
| 특징 | 프로젝트 레포의 일종이지만, 기본 구조만 갖추고 지식 레포가 핵심 |

---

## 레포 간 관계

### 데이터 흐름

```
④ 프로젝트 레포 ──(코드 작성)──→ ③ 지식 레포 (문서 자동 생성)
       │                                │
       │                                ├──→ ⑤ RAG 레포 (문서 인덱싱)
       │                                │
       ▼                                ▼
② 테스트 레포 ←──(코드 읽기, 문서 읽기)──┘
       │
       │  테스트 결과
       ▼
④ 프로젝트 레포 ←──("테스트 결과 피드백 반영" Hook)
       │
       │  수정 완료 → PR 생성
       ▼
GitHub Actions
  ├── feature → dev : GPT Codex (코드 리뷰)
  └── dev → main : Claude (보안 리뷰)
```

### 권한 매트릭스

| 레포 | → 프로젝트 | → 지식 | → 테스트 | → 거버넌스 | → RAG |
|------|-----------|--------|---------|-----------|-------|
| **프로젝트** | 읽기/쓰기 | **쓰기** | 읽기 | 읽기 | - |
| **지식** | 읽기 | 읽기/쓰기 | - | 읽기 | - |
| **테스트** | **읽기** | **읽기** | 읽기/쓰기 | 읽기 | - |
| **거버넌스** | - | - | - | 읽기/쓰기 | - |
| **RAG** | - | **읽기** | - | 읽기 | 읽기/쓰기 |

**핵심 규칙:**
- 테스트 레포 → 프로젝트 레포: **읽기 전용** (절대 수정 금지)
- 테스트 레포 → 지식 레포: **읽기 전용** (기능명세서 참조)
- 프로젝트 레포 → 지식 레포: **쓰기** (문서 자동 생성)
- RAG 레포 → 지식 레포: **읽기 전용** (문서 인덱싱)

---

## 프롬프트 관리

### 사용자 프롬프트 발생 지점

| 레포 | 프롬프트 빈도 | 상황 |
|------|-------------|------|
| ④ 프로젝트 | **높음** | 기능 개발, 버그 수정, 리팩토링 |
| ② 테스트 | **낮음** | 테스트 템플릿 오작동 시에만 |
| ③ 지식 | 거의 없음 | 문서 구조 변경 시 |
| ① 거버넌스 | 거의 없음 | Hook/규칙 추가/수정 시 |
| ⑤ RAG | 거의 없음 | 파이프라인 설정 시 |

### 프롬프트 로깅

```
사용자 프롬프트 입력
  │
  ▼
거버넌스 레포의 Hook이 가로챔
  │
  ├── 방식 A: 별도 DB에 저장 (구조화된 검색/분석 가능)
  └── 방식 B: 일자별 문서화 (logs/{YYYY-MM-DD}/prompts.md)
  │
  ▼
목적: 개인의 프롬프트 질을 올리기 위한 데이터 축적
  │
  ├── 어떤 프롬프트가 좋은 결과를 냈는가
  ├── 반복되는 패턴은 Hook/규칙으로 자동화할 수 있는가
  └── 시간 대비 효율이 낮은 프롬프트는 무엇인가
```

---

## PR 리뷰 파이프라인

```
feature 브랜치 → dev 브랜치 (PR)
  │
  ▼
GPT Codex (1차 리뷰)
  - 코드 품질, 패턴, 로직 오류
  - PR 코멘트로 피드백
  │
  ▼
수정 사항 반영
  │
  ▼
dev 브랜치 → main 브랜치 (PR)
  │
  ▼
Claude 보안 에이전트 (2차 리뷰)
  - OWASP Top 10, 크리덴셜 노출
  - 보안 취약점 분석
  │
  ▼
최종 머지
```

---

## 시각화 대시보드

레포만으로는 전체 시스템 상태를 볼 수 없다. 대시보드가 필요한 이유:

| 대시보드 항목 | 데이터 소스 | 보여줄 것 |
|-------------|-----------|----------|
| 테스트 현황 | 테스트 레포 results/ | 환경별 성공/실패율, 최근 테스트 이력 |
| 문서 현황 | 지식 레포 | 문서 커버리지, 최근 업데이트, 누락된 문서 |
| 프롬프트 분석 | 프롬프트 로그 | 일별 프롬프트 수, 패턴 분석, 품질 추이 |
| PR 리뷰 현황 | GitHub API | 리뷰 통과율, 주요 이슈 패턴 |
| 코드 변경 추이 | 프로젝트 레포 | 커밋 빈도, 변경 파일 수, 핫스팟 |
| 시스템 건강도 | 전체 | Hook 실행 성공률, 에러 로그 |

---

## 재사용 시나리오

### "새로운 프로젝트 시작" 시

```
① 거버넌스 레포    → 그대로 사용
② 테스트 레포      → .env만 교체 → setup.sh 실행
③ 지식 레포        → 새 프로젝트용 빈 구조 생성
④ 프로젝트 레포    → 🆕 새로 생성 (유일하게 새로 만드는 것)
⑤ RAG 레포         → 지식 레포 경로만 변경
```

**교체 비용:**
- .env 수정: 5분
- setup.sh 실행: 10초
- 지식 레포 초기화: 자동 (setup.sh)
- 프로젝트 레포 생성: 새 서비스 개발 시작

---

## 현재 상태 매핑

| 레포 | 상태 | 있는 것 | 없는 것 |
|------|------|---------|---------|
| ① 거버넌스 | 부분 완성 | Hook 스크립트, settings.json, PR YAML | 프롬프트 로깅 Hook, 대시보드 |
| ② 테스트 | 범용화 완료 | .env+envsubst 구조, rules/, templates/ | — |
| ③ 지식 | ERP용 존재 | 기능명세서, 인사이트 등 (ERP 특화) | 범용 템플릿 |
| ④ 프로젝트 | ERP용 존재 | CLAUDE.md, 개발 코드 | 범용 템플릿 |
| ⑤ RAG | 미착수 | — | 전부 |
| 대시보드 | 미착수 | — | 전부 |
| PR 리뷰 | 코드 완성 | Codex YAML, Claude YAML | GitHub Secrets 등록 |
